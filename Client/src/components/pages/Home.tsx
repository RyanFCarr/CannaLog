const Layout: React.FC = () => {
    return <Home />;
};

const Home: React.FC = () => {
    return <>Home</>;
};

const Welcome: React.FC = () => {
    return <>Onboarding Screen</>;
};

export default Layout;
